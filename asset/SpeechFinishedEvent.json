{
    "event": {
        "header": {
            "namespace": "SpeechSynthesizer",
            "name": "SpeechFinished",
            "messageId": "{{MESSAGE_ID_STRING}}"
        },
        "payload": {
            "token": "{{TOKEN_STRING}}"
        }
    }
}
