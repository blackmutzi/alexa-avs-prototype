{
    "event": {
        "header": {
            "namespace": "Alerts",
            "name": "{{EVENT_NAME}}",
            "messageId": "{{MESSAGE_ID_STRING}}"
        },
        "payload": {
            "token": "{{TOKEN_STRING}}"
        }
    }
}
