{
     "header": {
          "namespace": "SpeechSynthesizer",
          "name": "SpeechState"
      },
      "payload": {
          "token": "{{TOKEN_STRING}}",
          "offsetInMilliseconds": "{{MS}}",
          "playerActivity": "{{ACTIVITY_STRING}}"
      }
}
